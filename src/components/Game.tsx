import {shapes} from "@/store/game";

import {RockIcon} from "./RockIcon";

export function Game() {
  return (
    <section>
      <button type="button">
        <RockIcon />
      </button>
    </section>
  );
}
